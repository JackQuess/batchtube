{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'npm run prisma:generate && npm run prisma:migrate && node dist/${SERVICE_ENTRY:-server}.js'"
  }
}
