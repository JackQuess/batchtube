{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'ENTRY=${SERVICE_ENTRY:-server}; if [ \"$ENTRY\" = \"worker\" ]; then ENTRY=queues/worker; fi; npm run prisma:generate; if [ ! -f \"dist/${ENTRY}.js\" ]; then npm run build; fi; if [ \"$SERVICE_ENTRY\" != \"worker\" ]; then npm run prisma:migrate; fi; node \"dist/${ENTRY}.js\"'"
  }
}
