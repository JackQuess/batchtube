[phases.setup]
nixPkgs = ["nodejs_20", "python3", "ffmpeg"]

[phases.install]
cmds = [
  "npm install",
  "pip3 install --no-cache-dir yt-dlp"
]

[start]
cmd = "sh -c 'node src/${SERVICE_ENTRY:-server}.js'"

[variables]
NODE_ENV = "production"
