[phases.setup]
<<<<<<< HEAD
nixPkgs = ["nodejs_20", "python3", "ffmpeg"]

[phases.install]
cmds = [
  "npm install",
  "pip3 install --no-cache-dir yt-dlp"
]

[start]
cmd = "node src/server.js"

[variables]
NODE_ENV = "production"

=======
nixPkgs = ["nodejs_20", "python3", "pip"]

[phases.install]
cmds = [
  "npm ci",
  "pip3 install --upgrade yt-dlp || echo 'Warning: yt-dlp installation failed, some features may not work'"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "npm start"
>>>>>>> 69f9136 (Initial commit)
